# 全局搜索页面原型设计

## 页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│ Header: K8s管理平台 | 全局搜索框 | 用户头像▼                      │
├─────────────────────────────────────────────────────────────────┤
│ Sidebar │ Content Area                                          │
│ ├集群管理 │ ┌─────────────────────────────────────────────────┐ │
│ ├节点管理 │ │ 全局搜索                                         │ │
│ ├工作负载 │ │                                                 │ │
│ ├Pod管理  │ │ ┌─────────────────────────────────────────────┐ │ │
│ ├全局搜索 │ │ │🔍 搜索K8s资源、配置、日志...                 │ │ │
│ └审计日志 │ │ │                                             │ │ │
│          │ │ └─────────────────────────────────────────────┘ │ │
│          │ │                                                 │ │
│          │ │ ┌─────────────────────────────────────────────┐ │ │
│          │ │ │ 搜索结果 (125个)                             │ │ │
│          │ │ │ ┌─── 筛选 ───────────────────────────────┐   │ │ │
│          │ │ │ │资源类型▼│命名空间▼│集群▼│时间范围▼│     │   │ │ │
│          │ │ │ └─────────────────────────────────────────┘   │ │ │
│          │ │ │                                             │ │ │
│          │ │ │ 🔵 Pod (45个)                               │ │ │
│          │ │ │ 🟢 Deployment (12个)                        │ │ │
│          │ │ │ 🟡 Service (8个)                            │ │ │
│          │ │ │ 🟠 ConfigMap (15个)                         │ │ │
│          │ │ │ 🔴 Node (5个)                               │ │ │
│          │ │ └─────────────────────────────────────────────┘ │ │
│          │ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 功能模块

### 1. 搜索输入框
```
┌─────────────────────────────────────────────────────────────┐
│ 全局搜索                                                     │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🔍 搜索K8s资源、配置、日志...                            │ │
│ │                                                         │ │
│ │ 搜索建议:                                               │ │
│ │ • nginx (搜索包含nginx的资源)                           │ │
│ │ • kind:pod (搜索所有Pod)                               │ │
│ │ • namespace:default (搜索default命名空间)               │ │
│ │ • status:failed (搜索失败状态的资源)                    │ │
│ │ • label:app=nginx (按标签搜索)                         │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 高级搜索:                                                    │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│ │ 语法帮助  │ │ 搜索历史  │ │ 保存搜索  │ │ 清空     │       │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
```

### 2. 搜索筛选器
```
┌─────────────────────────────────────────────────────────────┐
│ 搜索筛选                                                     │
│                                                             │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │ 资源类型     │ │ 命名空间     │ │ 集群         │            │
│ │ 全部        ▼│ │ 全部        ▼│ │ prod-k8s   ▼│            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
│                                                             │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │ 状态         │ │ 时间范围     │ │ 标签         │            │
│ │ 全部        ▼│ │ 最近7天    ▼│ │ 添加筛选   ▼│            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
│                                                             │
│ 快速筛选:                                                    │
│ ☐ 只显示异常资源  ☐ 只显示系统资源  ☐ 最近创建               │
│                                                             │
│ ┌──────────┐ ┌──────────┐                                  │
│ │ 应用筛选  │ │ 重置筛选  │                                  │
│ └──────────┘ └──────────┘                                  │
└─────────────────────────────────────────────────────────────┘
```

### 3. 搜索结果概览
```
┌─────────────────────────────────────────────────────────────┐
│ 搜索结果统计                                                 │
│                                                             │
│ 关键词: "nginx" | 找到 125 个结果 | 耗时 0.23秒              │
│                                                             │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │ Pod         │ │ Deployment  │ │ Service     │            │
│ │ 45个        │ │ 12个        │ │ 8个         │            │
│ │ 🔵 36%      │ │ 🟢 9.6%     │ │ 🟡 6.4%     │            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
│                                                             │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │ ConfigMap   │ │ Secret      │ │ 其他        │            │
│ │ 15个        │ │ 10个        │ │ 35个        │            │
│ │ 🟠 12%      │ │ 🔴 8%       │ │ ⚪ 28%      │            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
│                                                             │
│ 命名空间分布:                                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ default: 45个 ████████████████████████████████████████░░ │ │
│ │ kube-system: 35个 ████████████████████████████░░░░░░░░░░ │ │
│ │ monitoring: 25个 ████████████████████░░░░░░░░░░░░░░░░░░░ │ │
│ │ ingress-nginx: 20个 ██████████████░░░░░░░░░░░░░░░░░░░░░░ │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 4. 搜索结果列表
```
┌─────────────────────────────────────────────────────────────┐
│ 搜索结果列表                                                 │
│                                                             │
│ ┌─────────────────┐ ┌──────────┐ ┌──────────┐             │
│ │ 排序: 相关性    ▼│ │ 视图: 列表│ │ 导出结果  │             │
│ └─────────────────┘ └──────────┘ └──────────┘             │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🔵 Pod | nginx-deploy-xxx-abc                           │ │
│ │ default 命名空间 | worker-1 节点 | Running               │ │
│ │ 匹配: 名称包含"nginx" | 标签app=nginx                    │ │
│ │ 创建时间: 2024-08-01 10:30:00 | 最后更新: 2小时前       │ │
│ │ ┌──────────┐ ┌──────────┐                              │ │
│ │ │ 查看详情  │ │ 快速操作  │                              │ │
│ │ └──────────┘ └──────────┘                              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🟢 Deployment | nginx-deployment                        │ │
│ │ default 命名空间 | 3/3 副本就绪                         │ │
│ │ 匹配: 名称包含"nginx" | 镜像nginx:1.20-alpine           │ │
│ │ 创建时间: 2024-07-15 14:20:00 | 最后更新: 1天前         │ │
│ │ ┌──────────┐ ┌──────────┐                              │ │
│ │ │ 查看详情  │ │ 快速操作  │                              │ │
│ │ └──────────┘ └──────────┘                              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🟡 Service | nginx-service                              │ │
│ │ default 命名空间 | ClusterIP | 80:30080/TCP             │ │
│ │ 匹配: 名称包含"nginx" | 选择器app=nginx                  │ │
│ │ 创建时间: 2024-07-15 14:25:00 | 最后更新: 1天前         │ │
│ │ ┌──────────┐ ┌──────────┐                              │ │
│ │ │ 查看详情  │ │ 快速操作  │                              │ │
│ │ └──────────┘ └──────────┘                              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🟠 ConfigMap | nginx-config                             │ │
│ │ default 命名空间 | 2个数据项                            │ │
│ │ 匹配: 名称包含"nginx" | 数据包含nginx配置                │ │
│ │ 创建时间: 2024-07-15 14:15:00 | 最后更新: 3天前         │ │
│ │ ┌──────────┐ ┌──────────┐                              │ │
│ │ │ 查看详情  │ │ 快速操作  │                              │ │
│ │ └──────────┘ └──────────┘                              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 显示 1-10 / 共 125 条                                       │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ ◀ 上一页  1  2  3  4  5  ...  13  下一页 ▶  每页10条 ▼│   │
│ └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 5. 搜索语法帮助
```
┌─────────────────────────────────────────────────────────────┐
│ 搜索语法帮助                                                 │
│                                                             │
│ 基本搜索:                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ nginx                    - 搜索包含"nginx"的所有资源     │ │
│ │ "nginx deployment"       - 精确匹配短语                 │ │
│ │ nginx OR apache          - 包含nginx或apache            │ │
│ │ nginx AND deployment     - 同时包含nginx和deployment    │ │
│ │ nginx NOT test           - 包含nginx但不包含test        │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 字段搜索:                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ kind:pod                 - 搜索Pod类型资源              │ │
│ │ namespace:default        - 搜索default命名空间          │ │
│ │ name:nginx*              - 名称以nginx开头             │ │
│ │ status:running           - 状态为running               │ │
│ │ node:worker-1            - 运行在worker-1节点          │ │
│ │ image:nginx:*            - 使用nginx镜像               │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 标签搜索:                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ label:app=nginx          - 标签app等于nginx            │ │
│ │ label:version!=v1.0      - 标签version不等于v1.0       │ │
│ │ label:env                - 存在env标签                 │ │
│ │ label:!debug             - 不存在debug标签             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 时间搜索:                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ created:>2024-08-01      - 2024-08-01之后创建          │ │
│ │ updated:<1d              - 1天内更新                   │ │
│ │ age:>7d                  - 存在超过7天                 │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 组合搜索示例:                                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ kind:pod AND status:failed AND namespace:default       │ │
│ │ label:app=nginx AND created:>2024-08-01                │ │
│ │ (kind:deployment OR kind:statefulset) AND image:nginx  │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 6. 搜索历史
```
┌─────────────────────────────────────────────────────────────┐
│ 搜索历史                                                     │
│                                                             │
│ 最近搜索:                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🕐 nginx                                    (2分钟前)   │ │
│ │ 🕐 kind:pod AND status:failed               (1小时前)   │ │
│ │ 🕐 label:app=api                            (3小时前)   │ │
│ │ 🕐 namespace:kube-system                    (1天前)     │ │
│ │ 🕐 image:redis                              (2天前)     │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 保存的搜索:                                                  │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ⭐ 失败的Pod                                             │ │
│ │    kind:pod AND status:failed                           │ │
│ │ ⭐ 生产环境资源                                          │ │
│ │    label:env=production                                 │ │
│ │ ⭐ 高资源使用                                            │ │
│ │    cpu:>80% OR memory:>80%                              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│ │ 清空历史  │ │ 导出历史  │ │ 管理收藏  │                    │
│ └──────────┘ └──────────┘ └──────────┘                    │
└─────────────────────────────────────────────────────────────┘
```

### 7. 快速操作面板
```
┌─────────────────────────────────────────────────────────────┐
│ 快速操作 (nginx-deploy-xxx-abc)                              │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📋 查看详情                                              │ │
│ │ 📊 查看监控                                              │ │
│ │ 📝 查看日志                                              │ │
│ │ 🖥️ 进入终端                                              │ │
│ │ ─────────────────────────────────────────────────────── │ │
│ │ 🔄 重启资源                                              │ │
│ │ 📤 导出YAML                                              │ │
│ │ 📋 复制名称                                              │ │
│ │ ─────────────────────────────────────────────────────── │ │
│ │ 🔗 查看关联资源                                          │ │
│ │ 📈 查看事件历史                                          │ │
│ │ 🏷️ 编辑标签                                              │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## Ant Design 组件映射

```jsx
<PageContainer
  header={{
    title: '全局搜索',
    subTitle: '搜索K8s集群中的所有资源'
  }}
>
  <Card style={{ marginBottom: 16 }}>
    <Input.Search
      placeholder="搜索K8s资源、配置、日志..."
      size="large"
      value={searchQuery}
      onChange={(e) => setSearchQuery(e.target.value)}
      onSearch={handleSearch}
      enterButton="搜索"
      style={{ marginBottom: 16 }}
    />
    
    <Row gutter={16}>
      <Col span={4}>
        <Select
          placeholder="资源类型"
          style={{ width: '100%' }}
          value={resourceType}
          onChange={setResourceType}
        >
          <Option value="">全部</Option>
          <Option value="pod">Pod</Option>
          <Option value="deployment">Deployment</Option>
          <Option value="service">Service</Option>
        </Select>
      </Col>
      <Col span={4}>
        <Select
          placeholder="命名空间"
          style={{ width: '100%' }}
          value={namespace}
          onChange={setNamespace}
        >
          <Option value="">全部</Option>
          <Option value="default">default</Option>
          <Option value="kube-system">kube-system</Option>
        </Select>
      </Col>
      <Col span={4}>
        <Select
          placeholder="状态"
          style={{ width: '100%' }}
          value={status}
          onChange={setStatus}
        >
          <Option value="">全部</Option>
          <Option value="running">运行中</Option>
          <Option value="failed">失败</Option>
        </Select>
      </Col>
      <Col span={4}>
        <DatePicker.RangePicker
          style={{ width: '100%' }}
          value={dateRange}
          onChange={setDateRange}
        />
      </Col>
      <Col span={8}>
        <Space>
          <Button onClick={handleApplyFilter}>应用筛选</Button>
          <Button onClick={handleResetFilter}>重置</Button>
          <Button icon={<HistoryOutlined />} onClick={showSearchHistory}>
            搜索历史
          </Button>
          <Button icon={<QuestionCircleOutlined />} onClick={showSyntaxHelp}>
            语法帮助
          </Button>
        </Space>
      </Col>
    </Row>
  </Card>

  {/* 搜索结果统计 */}
  <Row gutter={16} style={{ marginBottom: 16 }}>
    <Col span={4}>
      <Card>
        <Statistic
          title="Pod"
          value={searchStats.pod}
          prefix={<AppstoreOutlined style={{ color: '#1890ff' }} />}
        />
      </Card>
    </Col>
    <Col span={4}>
      <Card>
        <Statistic
          title="Deployment"
          value={searchStats.deployment}
          prefix={<DeploymentUnitOutlined style={{ color: '#52c41a' }} />}
        />
      </Card>
    </Col>
    <Col span={4}>
      <Card>
        <Statistic
          title="Service"
          value={searchStats.service}
          prefix={<ApiOutlined style={{ color: '#faad14' }} />}
        />
      </Card>
    </Col>
    <Col span={4}>
      <Card>
        <Statistic
          title="ConfigMap"
          value={searchStats.configmap}
          prefix={<SettingOutlined style={{ color: '#fa8c16' }} />}
        />
      </Card>
    </Col>
    <Col span={8}>
      <Card title="命名空间分布">
        <div className="namespace-chart">
          {namespaceDistribution.map(ns => (
            <div key={ns.name} className="namespace-item">
              <span>{ns.name}: {ns.count}个</span>
              <Progress
                percent={ns.percentage}
                size="small"
                showInfo={false}
              />
            </div>
          ))}
        </div>
      </Card>
    </Col>
  </Row>

  {/* 搜索结果列表 */}
  <Card>
    <div className="search-results-header">
      <div className="results-info">
        找到 <Text strong>{searchResults.total}</Text> 个结果，耗时 {searchResults.duration}ms
      </div>
      <div className="results-actions">
        <Space>
          <Select
            value={sortBy}
            onChange={setSortBy}
            style={{ width: 120 }}
          >
            <Option value="relevance">相关性</Option>
            <Option value="name">名称</Option>
            <Option value="created">创建时间</Option>
            <Option value="updated">更新时间</Option>
          </Select>
          <Button icon={<TableOutlined />} type={viewMode === 'list' ? 'primary' : 'default'}>
            列表
          </Button>
          <Button icon={<AppstoreOutlined />} type={viewMode === 'card' ? 'primary' : 'default'}>
            卡片
          </Button>
          <Button icon={<ExportOutlined />}>导出</Button>
        </Space>
      </div>
    </div>

    <List
      itemLayout="vertical"
      dataSource={searchResults.items}
      pagination={{
        total: searchResults.total,
        pageSize: 10,
        showSizeChanger: true,
        showQuickJumper: true,
        showTotal: (total, range) =>
          `显示 ${range[0]}-${range[1]} / 共 ${total} 条`,
      }}
      renderItem={(item) => (
        <List.Item
          key={item.id}
          actions={[
            <Button type="link" onClick={() => handleViewDetail(item)}>
              查看详情
            </Button>,
            <Dropdown overlay={getQuickActionsMenu(item)}>
              <Button type="link">
                快速操作 <DownOutlined />
              </Button>
            </Dropdown>
          ]}
        >
          <List.Item.Meta
            avatar={getResourceIcon(item.kind)}
            title={
              <Space>
                <Text strong>{item.name}</Text>
                <Tag color={getResourceColor(item.kind)}>{item.kind}</Tag>
                <Tag>{item.namespace}</Tag>
                {item.status && (
                  <Badge
                    status={getStatusBadge(item.status)}
                    text={item.status}
                  />
                )}
              </Space>
            }
            description={
              <div>
                <Text type="secondary">
                  匹配: {item.matchReason} | 
                  创建: {item.createdAt} | 
                  更新: {item.updatedAt}
                </Text>
                {item.labels && (
                  <div style={{ marginTop: 4 }}>
                    {Object.entries(item.labels).map(([key, value]) => (
                      <Tag key={key} size="small">
                        {key}={value}
                      </Tag>
                    ))}
                  </div>
                )}
              </div>
            }
          />
        </List.Item>
      )}
    />
  </Card>
</PageContainer>
```

## 数据结构

```typescript
interface SearchResult {
  id: string;
  name: string;
  kind: string;
  namespace: string;
  cluster: string;
  status?: string;
  createdAt: string;
  updatedAt: string;
  matchReason: string;
  labels: Record<string, string>;
  annotations: Record<string, string>;
  relevanceScore: number;
}

interface SearchStats {
  total: number;
  duration: number;
  pod: number;
  deployment: number;
  service: number;
  configmap: number;
  secret: number;
  node: number;
  other: number;
}

interface SearchFilter {
  query: string;
  resourceType: string;
  namespace: string;
  cluster: string;
  status: string;
  labels: string[];
  dateRange: [string, string];
  showSystemResources: boolean;
  showFailedOnly: boolean;
  recentOnly: boolean;
}

interface SearchHistory {
  id: string;
  query: string;
  timestamp: string;
  resultCount: number;
  saved: boolean;
  name?: string;
}
```

## 搜索功能特性

### 1. 智能搜索
- 自动补全和建议
- 模糊匹配和拼写纠正
- 语义搜索和关联推荐

### 2. 高级筛选
- 多维度筛选组合
- 自定义标签筛选
- 时间范围筛选

### 3. 搜索历史
- 自动保存搜索历史
- 收藏常用搜索
- 搜索模板管理

### 4. 结果展示
- 多种视图模式
- 相关性排序
- 快速操作入口

## 响应式设计

### 移动端适配
- 搜索框优先显示
- 筛选条件折叠
- 结果卡片化展示

### 平板端适配
- 保持筛选栏布局
- 调整统计卡片排列
- 优化触摸操作

### 桌面端
- 完整功能展示
- 支持键盘快捷键
- 高级搜索语法
