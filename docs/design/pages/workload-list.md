# 工作负载列表页面原型设计

## 页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│ Header: K8s管理平台 | 全局搜索框 | 用户头像▼                      │
├─────────────────────────────────────────────────────────────────┤
│ Sidebar │ Content Area                                          │
│ ├集群管理 │ ┌─────────────────────────────────────────────────┐ │
│ ├节点管理 │ │ 工作负载 > prod-k8s > 工作负载列表               │ │
│ ├工作负载 │ │                                                 │ │
│ │├Deployment│ ┌─────────────────┐ ┌──────────┐ ┌──────────┐   │ │
│ │├StatefulSet│ │🔍 搜索工作负载... │ │命名空间▼  │ │+ 创建     │   │ │
│ │├DaemonSet │ │                 │ │          │ │          │   │ │
│ │├Job      │ └─────────────────┘ └──────────┘ └──────────┘   │ │
│ │└CronJob  │                                                 │ │
│ ├Pod管理  │ ┌─── 工作负载类型 ───────────────────────────────┐ │ │
│ ├全局搜索 │ │ Deployment│StatefulSet│DaemonSet│Job│CronJob │ │ │
│ └审计日志 │ │ [选中]    │          │         │   │       │ │ │
│          │ └─────────────────────────────────────────────────┘ │ │
│          │                                                 │ │
│          │ ┌─────────────────────────────────────────────┐ │ │
│          │ │ Deployment 列表                              │ │ │
│          │ │ ┌──┬────────────┬────┬────┬────┬────┬────┐ │ │ │
│          │ │ │状│名称         │NS  │副本│就绪│镜像│操作│ │ │ │
│          │ │ ├──┼────────────┼────┼────┼────┼────┼────┤ │ │ │
│          │ │ │🟢│nginx-deploy │def │3/3 │3   │nginx│... │ │ │ │
│          │ │ │🟡│api-server   │prod│2/3 │2   │api  │... │ │ │ │
│          │ │ │🟢│web-frontend │prod│5/5 │5   │web  │... │ │ │ │
│          │ │ │🔴│mysql-cluster│prod│0/1 │0   │mysql│... │ │ │ │
│          │ │ └──┴────────────┴────┴────┴────┴────┴────┘ │ │ │
│          │ └─────────────────────────────────────────────┘ │ │
│          │                                                 │ │
│          │ 分页: ← 1 2 3 ... 8 →                           │ │
│          └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 功能组件

### 1. 页面头部工具栏
- **搜索框**: 支持按名称、标签、镜像搜索
- **命名空间筛选**: 下拉选择或输入筛选
- **创建按钮**: 快速创建新的工作负载

### 2. 工作负载类型标签页
```
┌─────────────────────────────────────────────────────────────┐
│ 工作负载类型导航                                             │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │📦 Deployment │ │🗄️ StatefulSet│ │⚙️ DaemonSet  │            │
│ │ (12个)      │ │ (3个)       │ │ (8个)       │            │
│ │ [选中]      │ │             │ │             │            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
│ ┌─────────────┐ ┌─────────────┐                            │
│ │⏰ Job        │ │📅 CronJob    │                            │
│ │ (5个)       │ │ (2个)       │                            │
│ │             │ │             │                            │
│ └─────────────┘ └─────────────┘                            │
└─────────────────────────────────────────────────────────────┘
```

### 3. Deployment 列表表格
| 列名 | 宽度 | 说明 |
|------|------|------|
| 状态 | 60px | 🟢正常 🟡警告 🔴异常 ⏸️暂停 |
| 名称 | 200px | 可点击进入详情 |
| 命名空间 | 100px | 所属命名空间 |
| 副本状态 | 80px | 就绪/期望 (如: 3/3) |
| 就绪副本 | 80px | 当前就绪数量 |
| 镜像 | 150px | 主容器镜像，悬停显示完整 |
| 更新时间 | 120px | 最后更新时间 |
| 操作 | 150px | 扩缩容/编辑/删除 |

### 4. 状态指示器
- 🟢 **正常**: 所有副本就绪且健康
- 🟡 **警告**: 部分副本未就绪或有问题
- 🔴 **异常**: 无可用副本或严重错误
- ⏸️ **暂停**: 部署已暂停
- 🔄 **更新中**: 正在进行滚动更新

### 5. 快速操作菜单
```
┌─────────────────────────────────────────────────────────────┐
│ 操作菜单                                                     │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │ 📊 查看详情  │ │ 📝 编辑YAML  │ │ ⚙️ 更多操作  │            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
│                                                             │
│ 更多操作下拉菜单:                                           │
│ • 扩缩容                                                    │
│ • 重启部署                                                  │
│ • 暂停/恢复                                                 │
│ • 回滚版本                                                  │
│ • 查看事件                                                  │
│ • 查看日志                                                  │
│ • 删除                                                      │
└─────────────────────────────────────────────────────────────┘
```

## 不同工作负载类型的特殊显示

### StatefulSet 列表
```
┌─────────────────────────────────────────────────────────────┐
│ StatefulSet 列表                                            │
│ ┌──┬────────────┬────┬────┬────┬────────┬────────┬────┐   │
│ │状│名称         │NS  │副本│就绪│存储     │服务     │操作│   │
│ ├──┼────────────┼────┼────┼────┼────────┼────────┼────┤   │
│ │🟢│redis-cluster│prod│3/3 │3   │10Gi×3  │redis-svc│... │   │
│ │🟡│mysql-master │prod│1/1 │1   │50Gi    │mysql    │... │   │
│ │🟢│elasticsearch│logs│3/3 │3   │100Gi×3 │es-svc   │... │   │
│ └──┴────────────┴────┴────┴────┴────────┴────────┴────┘   │
└─────────────────────────────────────────────────────────────┘
```

### DaemonSet 列表
```
┌─────────────────────────────────────────────────────────────┐
│ DaemonSet 列表                                              │
│ ┌──┬────────────┬────┬────────┬────┬────────┬────────┐     │
│ │状│名称         │NS  │节点覆盖 │就绪│调度策略 │更新策略 │     │
│ ├──┼────────────┼────┼────────┼────┼────────┼────────┤     │
│ │🟢│fluentd     │logs│5/5     │5   │所有节点 │滚动更新 │     │
│ │🟢│node-exporter│mon │5/5     │5   │所有节点 │滚动更新 │     │
│ │🟡│filebeat    │logs│4/5     │4   │worker  │滚动更新 │     │
│ └──┴────────────┴────┴────────┴────┴────────┴────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### Job 列表
```
┌─────────────────────────────────────────────────────────────┐
│ Job 列表                                                    │
│ ┌──┬────────────┬────┬────────┬────┬────────┬────────┐     │
│ │状│名称         │NS  │完成情况 │并行│持续时间 │完成时间 │     │
│ ├──┼────────────┼────┼────────┼────┼────────┼────────┤     │
│ │✅│backup-job   │prod│1/1     │1   │2m30s   │5分钟前  │     │
│ │🔄│data-migrate │prod│2/3     │3   │15m     │运行中   │     │
│ │❌│cleanup-job  │prod│0/1     │1   │30s     │失败     │     │
│ └──┴────────────┴────┴────────┴────┴────────┴────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### CronJob 列表
```
┌─────────────────────────────────────────────────────────────┐
│ CronJob 列表                                                │
│ ┌──┬────────────┬────┬────────┬────────┬────────┬────┐     │
│ │状│名称         │NS  │调度表达式│上次运行 │下次运行 │状态│     │
│ ├──┼────────────┼────┼────────┼────────┼────────┼────┤     │
│ │🟢│daily-backup │prod│0 2 * * *│2小时前  │22小时后 │活跃│     │
│ │⏸️│weekly-report│prod│0 0 * * 0│7天前   │暂停     │暂停│     │
│ └──┴────────────┴────┴────────┴────────┴────────┴────┘     │
└─────────────────────────────────────────────────────────────┘
```

## 批量操作功能

### 批量选择和操作
```
┌─────────────────────────────────────────────────────────────┐
│ 批量操作栏 (选中工作负载时显示)                              │
│ 已选中 3 个 Deployment  [全选] [取消]                       │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │ 批量扩缩容   │ │ 批量重启     │ │ 批量删除     │            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
└─────────────────────────────────────────────────────────────┘
```

## 筛选和搜索功能

### 高级筛选面板
```
┌─────────────────────────────────────────────────────────────┐
│ 高级筛选                                                     │
│                                                             │
│ 命名空间: ┌─────────────────┐ 状态: ┌─────────────────┐     │
│          │ default, prod  ▼│      │ 全部           ▼│     │
│          └─────────────────┘      └─────────────────┘     │
│                                                             │
│ 标签选择器:                                                  │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ app=nginx,version=v1.0                                  │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 镜像筛选:                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ nginx:1.20                                              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 创建时间: ┌─────────────┐ 到 ┌─────────────┐              │
│          │ 2024-08-01  │   │ 2024-08-15  │              │
│          └─────────────┘   └─────────────┘              │
│                                                             │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│ │ 重置      │ │ 应用筛选  │ │ 保存筛选  │                    │
│ └──────────┘ └──────────┘ └──────────┘                    │
└─────────────────────────────────────────────────────────────┘
```

## 快速创建面板

### 创建工作负载向导
```
┌─────────────────────────────────────────────────────────────┐
│ 创建工作负载                                                 │
│                                                             │
│ 选择类型:                                                    │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │📦 Deployment │ │🗄️ StatefulSet│ │⚙️ DaemonSet  │            │
│ │ 无状态应用   │ │ 有状态应用   │ │ 系统服务     │            │
│ │ [选中]      │ │             │ │             │            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
│ ┌─────────────┐ ┌─────────────┐                            │
│ │⏰ Job        │ │📅 CronJob    │                            │
│ │ 批处理任务   │ │ 定时任务     │                            │
│ │             │ │             │                            │
│ └─────────────┘ └─────────────┘                            │
│                                                             │
│ 快速模板:                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ • Nginx Web服务器                                        │ │
│ │ • Redis 缓存服务                                         │ │
│ │ • MySQL 数据库                                           │ │
│ │ • 自定义应用                                             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌──────────┐ ┌──────────┐                                  │
│ │ 取消      │ │ 下一步    │                                  │
│ └──────────┘ └──────────┘                                  │
└─────────────────────────────────────────────────────────────┘
```

## Ant Design 组件映射

```jsx
<PageContainer
  header={{
    title: '工作负载管理',
    breadcrumb: {
      routes: [
        { path: '/clusters', breadcrumbName: '集群管理' },
        { path: '/clusters/prod-k8s', breadcrumbName: 'prod-k8s' },
        { path: '/workloads', breadcrumbName: '工作负载' }
      ]
    }
  }}
>
  {/* 工具栏 */}
  <Row gutter={16} style={{ marginBottom: 16 }}>
    <Col span={8}>
      <Input.Search 
        placeholder="搜索工作负载名称、标签..." 
        onSearch={handleSearch}
      />
    </Col>
    <Col span={6}>
      <Select 
        mode="multiple"
        placeholder="选择命名空间"
        style={{ width: '100%' }}
        options={namespaceOptions}
      />
    </Col>
    <Col span={4}>
      <Button type="primary" icon={<PlusOutlined />}>
        创建工作负载
      </Button>
    </Col>
    <Col span={6}>
      <Button icon={<FilterOutlined />} onClick={showAdvancedFilter}>
        高级筛选
      </Button>
    </Col>
  </Row>

  {/* 工作负载类型标签页 */}
  <Tabs 
    activeKey={workloadType} 
    onChange={setWorkloadType}
    tabBarExtraContent={
      <Badge count={totalCount} showZero>
        <Button icon={<ReloadOutlined />} onClick={handleRefresh}>
          刷新
        </Button>
      </Badge>
    }
  >
    <TabPane 
      tab={<span><AppstoreOutlined />Deployment ({deploymentCount})</span>} 
      key="deployment"
    >
      <Table
        rowSelection={{
          type: 'checkbox',
          onChange: handleSelectionChange
        }}
        columns={deploymentColumns}
        dataSource={deploymentData}
        pagination={{
          showSizeChanger: true,
          showQuickJumper: true,
          showTotal: (total) => `共 ${total} 个 Deployment`
        }}
      />
    </TabPane>
    
    <TabPane 
      tab={<span><DatabaseOutlined />StatefulSet ({statefulSetCount})</span>} 
      key="statefulset"
    >
      <Table
        columns={statefulSetColumns}
        dataSource={statefulSetData}
        pagination={false}
      />
    </TabPane>
    
    <TabPane 
      tab={<span><SettingOutlined />DaemonSet ({daemonSetCount})</span>} 
      key="daemonset"
    >
      <Table
        columns={daemonSetColumns}
        dataSource={daemonSetData}
        pagination={false}
      />
    </TabPane>
    
    <TabPane 
      tab={<span><ClockCircleOutlined />Job ({jobCount})</span>} 
      key="job"
    >
      <Table
        columns={jobColumns}
        dataSource={jobData}
        pagination={false}
      />
    </TabPane>
    
    <TabPane 
      tab={<span><ScheduleOutlined />CronJob ({cronJobCount})</span>} 
      key="cronjob"
    >
      <Table
        columns={cronJobColumns}
        dataSource={cronJobData}
        pagination={false}
      />
    </TabPane>
  </Tabs>

  {/* 批量操作栏 */}
  {selectedWorkloads.length > 0 && (
    <Card style={{ position: 'fixed', bottom: 20, left: 20, right: 20 }}>
      <Row justify="space-between" align="middle">
        <Col>
          已选中 {selectedWorkloads.length} 个工作负载
        </Col>
        <Col>
          <Space>
            <Button onClick={handleBatchScale}>批量扩缩容</Button>
            <Button onClick={handleBatchRestart}>批量重启</Button>
            <Button danger onClick={handleBatchDelete}>批量删除</Button>
          </Space>
        </Col>
      </Row>
    </Card>
  )}
</PageContainer>
```

## 数据结构

```typescript
interface WorkloadListItem {
  // 通用字段
  kind: 'Deployment' | 'StatefulSet' | 'DaemonSet' | 'Job' | 'CronJob';
  metadata: {
    name: string;
    namespace: string;
    uid: string;
    creationTimestamp: string;
    labels: Record<string, string>;
    annotations: Record<string, string>;
  };
  
  // 状态信息
  status: {
    phase: string;
    replicas?: number;
    readyReplicas?: number;
    updatedReplicas?: number;
    availableReplicas?: number;
  };
  
  // 规格信息
  spec: {
    replicas?: number;
    selector: LabelSelector;
    template: PodTemplateSpec;
  };
  
  // 特定类型字段
  deploymentStatus?: DeploymentStatus;
  statefulSetStatus?: StatefulSetStatus;
  daemonSetStatus?: DaemonSetStatus;
  jobStatus?: JobStatus;
  cronJobStatus?: CronJobStatus;
}

interface DeploymentStatus {
  conditions: DeploymentCondition[];
  strategy: DeploymentStrategy;
  rolloutHistory: RolloutRevision[];
}

interface StatefulSetStatus {
  serviceName: string;
  volumeClaimTemplates: PersistentVolumeClaimTemplate[];
  updateStrategy: StatefulSetUpdateStrategy;
}

interface DaemonSetStatus {
  currentNumberScheduled: number;
  desiredNumberScheduled: number;
  numberMisscheduled: number;
  numberReady: number;
  updateStrategy: DaemonSetUpdateStrategy;
}

interface JobStatus {
  active: number;
  succeeded: number;
  failed: number;
  startTime?: string;
  completionTime?: string;
  duration?: string;
}

interface CronJobStatus {
  schedule: string;
  lastScheduleTime?: string;
  nextScheduleTime?: string;
  active: JobReference[];
  suspend: boolean;
}
```

## 响应式设计

### 移动端适配
- 工作负载类型改为下拉选择
- 表格列合并显示关键信息
- 操作按钮改为抽屉式菜单

### 平板端适配
- 保持标签页布局
- 调整表格列宽
- 优化批量操作栏位置

### 桌面端
- 完整功能展示
- 支持多列排序
- 高级筛选面板