# 节点列表页面原型设计

## 页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│ Header: K8s管理平台 | 全局搜索框 | 用户头像▼                      │
├─────────────────────────────────────────────────────────────────┤
│ Sidebar │ Content Area                                          │
│ ├集群管理 │ ┌─────────────────────────────────────────────────┐ │
│ ├节点管理 │ │ 节点管理 > prod-k8s > 节点列表                   │ │
│ │├节点列表 │ │                                                 │ │
│ ├工作负载 │ │ ┌─────────────────┐ ┌──────────┐ ┌──────────┐   │ │
│ ├Pod管理  │ │ │🔍 搜索节点...    │ │状态: 全部▼│ │刷新      │   │ │
│ ├全局搜索 │ │ └─────────────────┘ └──────────┘ └──────────┘   │ │
│ └审计日志 │ │                                                 │ │
│          │ │ ┌─────────────────────────────────────────────┐ │ │
│          │ │ │ 节点概览                                     │ │ │
│          │ │ │ 总计: 5个节点 | 就绪: 5个 | 异常: 0个        │ │ │
│          │ │ │ CPU: 65% | 内存: 45% | 存储: 23%            │ │ │
│          │ │ └─────────────────────────────────────────────┘ │ │
│          │ │                                                 │ │
│          │ │ ┌─────────────────────────────────────────────┐ │ │
│          │ │ │ 节点列表                                     │ │ │
│          │ │ │ ┌──┬────────┬────┬────┬────┬────┬────┬────┐ │ │ │
│          │ │ │ │状│节点名称 │角色│版本│就绪│CPU │内存│操作│ │ │ │
│          │ │ │ ├──┼────────┼────┼────┼────┼────┼────┼────┤ │ │ │
│          │ │ │ │🟢│master-1 │M+W │1.28│✓  │65% │45% │... │ │ │ │
│          │ │ │ │🟢│worker-1 │W   │1.28│✓  │70% │50% │... │ │ │ │
│          │ │ │ │🟢│worker-2 │W   │1.28│✓  │60% │40% │... │ │ │ │
│          │ │ │ │🟡│worker-3 │W   │1.28│✓  │85% │75% │... │ │ │ │
│          │ │ │ │🟢│worker-4 │W   │1.28│✓  │55% │35% │... │ │ │ │
│          │ │ │ └──┴────────┴────┴────┴────┴────┴────┴────┘ │ │ │
│          │ │ └─────────────────────────────────────────────┘ │ │
│          │ │                                                 │ │
│          │ │ 分页: ← 1 2 3 ... 5 →                           │ │
│          │ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 功能组件

### 1. 页面头部工具栏
- **搜索框**: 支持按节点名称、IP、标签搜索
- **状态筛选**: 全部/就绪/未就绪/调度禁用/维护中
- **刷新按钮**: 手动刷新节点状态

### 2. 节点概览卡片
```
┌─────────────────────────────────────────────────────────────┐
│ 节点概览                                                     │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│ │ 总节点   │ │ 就绪节点 │ │ 异常节点 │ │ 维护节点 │            │
│ │   5     │ │   5     │ │   0     │ │   0     │            │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘            │
│                                                             │
│ 整体资源使用:                                               │
│ CPU: ████████░░ 65%  内存: ██████░░░░ 45%  存储: ███░░░░░░░ 23% │
└─────────────────────────────────────────────────────────────┘
```

### 3. 节点列表表格
| 列名 | 宽度 | 说明 |
|------|------|------|
| 状态 | 60px | 🟢就绪 🟡警告 🔴异常 ⚫维护 |
| 节点名称 | 150px | 可点击进入详情 |
| 角色 | 80px | M(Master) W(Worker) M+W(混合) |
| 版本 | 100px | kubelet版本 |
| 就绪状态 | 80px | ✓就绪 ✗未就绪 ⏸️调度禁用 |
| CPU使用率 | 100px | 进度条 + 百分比 |
| 内存使用率 | 100px | 进度条 + 百分比 |
| Pod数量 | 80px | 运行中/总容量 |
| 污点 | 100px | 污点数量，悬停显示详情 |
| 创建时间 | 120px | 相对时间 |
| 操作 | 150px | 详情/终端/Cordon/Drain |

### 4. 节点状态指示
- 🟢 **就绪**: 节点正常，可调度Pod
- 🟡 **警告**: 资源使用率高或有轻微问题
- 🔴 **异常**: 节点不可用或有严重问题
- ⚫ **维护**: 节点被标记为维护状态
- ⏸️ **调度禁用**: 节点被Cordon，不接受新Pod

### 5. 操作按钮组
```
┌─────────────────────────────────────────────────────────────┐
│ 操作菜单                                                     │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │ 📊 查看详情  │ │ 🖥️ 节点终端  │ │ ⚙️ 更多操作  │            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
│                                                             │
│ 更多操作下拉菜单:                                           │
│ • Cordon (禁用调度)                                         │
│ • Uncordon (启用调度)                                       │
│ • Drain (驱逐Pod)                                          │
│ • 编辑标签                                                  │
│ • 编辑污点                                                  │
│ • 查看事件                                                  │
└─────────────────────────────────────────────────────────────┘
```

## 交互设计

### 节点状态实时更新
- **自动刷新**: 每30秒自动更新节点状态
- **状态变化**: 状态改变时有视觉提示
- **连接状态**: 显示与节点的连接状态

### 资源使用率可视化
- **进度条**: 直观显示CPU/内存使用率
- **颜色编码**: 
  - 绿色: 0-60% (正常)
  - 黄色: 60-80% (警告)
  - 红色: 80-100% (危险)

### 批量操作
```
┌─────────────────────────────────────────────────────────────┐
│ 批量操作栏 (选中节点时显示)                                  │
│ 已选中 3 个节点  [全选] [取消]                              │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│ │ 批量Cordon   │ │ 批量Uncordon │ │ 批量添加标签 │            │
│ └─────────────┘ └─────────────┘ └─────────────┘            │
└─────────────────────────────────────────────────────────────┘
```

### 响应式设计
- **移动端**: 隐藏部分列，使用卡片布局
- **平板端**: 调整列宽，保持表格布局
- **桌面端**: 完整表格显示

## Ant Design 组件映射

```jsx
<PageContainer
  header={{
    title: '节点管理',
    breadcrumb: {
      routes: [
        { path: '/clusters', breadcrumbName: '集群管理' },
        { path: '/clusters/prod-k8s', breadcrumbName: 'prod-k8s' },
        { path: '/nodes', breadcrumbName: '节点列表' }
      ]
    }
  }}
>
  {/* 工具栏 */}
  <Row gutter={16} style={{ marginBottom: 16 }}>
    <Col span={8}>
      <Input.Search 
        placeholder="搜索节点名称、IP、标签..." 
        onSearch={handleSearch}
      />
    </Col>
    <Col span={4}>
      <Select 
        defaultValue="all" 
        style={{ width: '100%' }}
        options={statusOptions}
      />
    </Col>
    <Col span={4}>
      <Button icon={<ReloadOutlined />} onClick={handleRefresh}>
        刷新
      </Button>
    </Col>
  </Row>

  {/* 节点概览 */}
  <Card style={{ marginBottom: 16 }}>
    <Row gutter={16}>
      <Col span={6}>
        <Statistic title="总节点" value={5} />
      </Col>
      <Col span={6}>
        <Statistic title="就绪节点" value={5} valueStyle={{ color: '#3f8600' }} />
      </Col>
      <Col span={6}>
        <Statistic title="异常节点" value={0} valueStyle={{ color: '#cf1322' }} />
      </Col>
      <Col span={6}>
        <Statistic title="维护节点" value={0} />
      </Col>
    </Row>
    <Divider />
    <Row gutter={16}>
      <Col span={8}>
        <div>CPU使用率</div>
        <Progress percent={65} status="active" />
      </Col>
      <Col span={8}>
        <div>内存使用率</div>
        <Progress percent={45} />
      </Col>
      <Col span={8}>
        <div>存储使用率</div>
        <Progress percent={23} />
      </Col>
    </Row>
  </Card>

  {/* 节点列表 */}
  <Table
    rowSelection={{
      type: 'checkbox',
      onChange: handleSelectionChange
    }}
    columns={nodeColumns}
    dataSource={nodeData}
    pagination={{
      showSizeChanger: true,
      showQuickJumper: true,
      showTotal: (total) => `共 ${total} 个节点`
    }}
  />

  {/* 批量操作栏 */}
  {selectedNodes.length > 0 && (
    <Card style={{ position: 'fixed', bottom: 20, left: 20, right: 20 }}>
      <Row justify="space-between" align="middle">
        <Col>
          已选中 {selectedNodes.length} 个节点
        </Col>
        <Col>
          <Space>
            <Button onClick={handleBatchCordon}>批量Cordon</Button>
            <Button onClick={handleBatchUncordon}>批量Uncordon</Button>
            <Button onClick={handleBatchLabel}>批量添加标签</Button>
          </Space>
        </Col>
      </Row>
    </Card>
  )}
</PageContainer>
```

## 数据结构

```typescript
interface NodeListItem {
  id: string;
  name: string;
  status: 'ready' | 'notready' | 'unknown';
  roles: ('master' | 'worker')[];
  version: string;
  ready: boolean;
  schedulable: boolean;
  
  // 资源使用
  resources: {
    cpu: {
      used: number;
      total: number;
      percentage: number;
    };
    memory: {
      used: number;
      total: number;
      percentage: number;
    };
    pods: {
      current: number;
      capacity: number;
    };
  };
  
  // 节点信息
  nodeInfo: {
    kubeletVersion: string;
    osImage: string;
    architecture: string;
    containerRuntime: string;
  };
  
  // 污点和标签
  taints: K8sTaint[];
  labels: Record<string, string>;
  
  // 时间信息
  createdAt: string;
  lastHeartbeat: string;
  
  // 条件状态
  conditions: NodeCondition[];
}

interface NodeCondition {
  type: string;
  status: 'True' | 'False' | 'Unknown';
  reason?: string;
  message?: string;
  lastTransitionTime: string;
}

interface K8sTaint {
  key: string;
  value?: string;
  effect: 'NoSchedule' | 'PreferNoSchedule' | 'NoExecute';
}
```

## 特殊功能

### 1. 节点健康检查
```
┌─────────────────────────────────────────────────────────────┐
│ 节点健康状态详情 (悬停显示)                                  │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ✅ Ready: 节点健康且可调度                               │ │
│ │ ✅ MemoryPressure: False                                │ │
│ │ ✅ DiskPressure: False                                  │ │
│ │ ✅ PIDPressure: False                                   │ │
│ │ ✅ NetworkUnavailable: False                            │ │
│ │ 最后心跳: 30秒前                                        │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 2. 污点管理
```
┌─────────────────────────────────────────────────────────────┐
│ 污点信息 (点击查看)                                          │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ key=node-role.kubernetes.io/master:NoSchedule           │ │
│ │ key=maintenance:NoExecute (临时维护)                     │ │
│ │                                                         │ │
│ │ ┌─────────────┐ ┌─────────────┐                        │ │
│ │ │ 添加污点     │ │ 移除污点     │                        │ │
│ │ └─────────────┘ └─────────────┘                        │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 3. 快速操作确认
```
┌─────────────────────────────────────────────────────────────┐
│ 确认操作                                                     │
│                                                             │
│ 您确定要对节点 "worker-3" 执行 Drain 操作吗？                │
│                                                             │
│ ⚠️ 此操作将：                                                │
│ • 禁用节点调度 (Cordon)                                     │
│ • 驱逐所有非系统Pod                                         │
│ • 可能影响正在运行的服务                                     │
│                                                             │
│ 高级选项:                                                   │
│ ☑️ 忽略DaemonSet Pod                                        │
│ ☑️ 删除本地存储的Pod                                        │
│ ☐ 强制删除 (--force)                                       │
│ 宽限期: [30] 秒                                             │
│                                                             │
│ ┌─────────────┐ ┌─────────────┐                            │
│ │ 取消         │ │ 确认执行     │                            │
│ └─────────────┘ └─────────────┘                            │
└─────────────────────────────────────────────────────────────┘