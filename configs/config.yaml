# 服务器配置
server:
  port: 8080
  mode: debug  # debug, release

# 数据库配置 - 使用MySQL数据库
database:
  driver: mysql
  dsn: ""
  host: 127.0.0.1
  port: 3306
  username: kubepolaris
  password: kubepolaris!@#
  database: kubepolaris
  charset: utf8mb4

# JWT配置
jwt:
  secret: k8s-management-secret-key
  expire_time: 24  # 小时

# 日志配置
log:
  level: info  # debug, info, warn, error

# Kubernetes配置
k8s:
  default_namespace: default

# Grafana 配置（用于自动同步数据源）
grafana:
  enabled: true  # 是否启用 Grafana 数据源自动同步
  url: http://localhost:3000  # Grafana 地址
  api_key: "glsa_miZpnRNwA35GXcN4vCLfYoDXSnNFY37U_2b23f04e"  # Grafana Service Account Token（需要 Admin 权限）
  # 获取 API Key：Grafana -> Administration -> Service Accounts -> Add token